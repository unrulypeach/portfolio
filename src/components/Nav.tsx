const Nav = () => {
  return (
    <nav>
      <button>Home</button>
      <button>About</button>
      <button>Projects</button>
      <button>Contact</button>
    </nav>
  );
};

export default Nav;
